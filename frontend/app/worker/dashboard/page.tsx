import WorkerDashboard from '@/components/worker/dashboard'

export const metadata = {
  title: 'Dashboard Lavoratore — WorksiteID',
}

export default function Page() {
  return <WorkerDashboard />
}
